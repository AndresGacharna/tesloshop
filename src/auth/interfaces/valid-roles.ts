export enum ValidRoles{ // el enum lo que hace es que muestra los roles como números, por eso definimos el string. Porque así podremos llamarlos. admin (Esto se transforma en numero) = 'admin' así es como lo llamamos
    
//  numerico = 'como lo quiero llamar'
    admin = 'admin',
    superUser = 'super-user',
    user = 'user'
}